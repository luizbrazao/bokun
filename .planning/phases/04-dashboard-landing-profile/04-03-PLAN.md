---
phase: 04-dashboard-landing-profile
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/LandingPage.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
autonomous: true
requirements: [LAND-01, LAND-02, LAND-03, LAND-04]

must_haves:
  truths:
    - "Landing page is accessible at the public route / (unauthenticated users see it; authenticated users can also visit it)"
    - "Hero section shows the exact headline and subheadline from CONTEXT.md, and primary CTA 'Começar teste grátis' links to Bokun marketplace"
    - "Features section shows exactly 4 benefit cards with icons matching the 4 benefits from CONTEXT.md"
    - "Pricing section shows two tiers (Monthly €29/mês, Annual €290/ano) with 7-day trial note and CTA buttons"
    - "Landing page is fully readable on mobile (375px viewport) — no horizontal scroll, text is legible, CTAs are tappable"
  artifacts:
    - path: "frontend/src/pages/LandingPage.tsx"
      provides: "Complete marketing landing page (hero, features, pricing)"
      min_lines: 150
  key_links:
    - from: "frontend/src/pages/LandingPage.tsx"
      to: "Stripe Checkout"
      via: "href to /api/create-checkout-session (will be implemented in 04-04)"
      pattern: "create-checkout-session"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/LandingPage.tsx"
      via: "Route path='/'"
      pattern: "LandingPage"
---

<objective>
Build the public marketing landing page with hero, features, and pricing sections. This is independent of all other Phase 4 plans and runs in Wave 1 parallel with 04-01.

Purpose: Attract new vendors from the Bokun marketplace with a clear value proposition and direct Stripe Checkout links.
Output: LandingPage.tsx with three sections, public route at /, full mobile responsiveness.
</objective>

<execution_context>
@/Users/luizbrazao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luizbrazao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dashboard-landing-profile/04-CONTEXT.md
</context>

<interfaces>
<!-- Key decisions from CONTEXT.md — executor must use these exactly. -->

Hero section (LAND-01):
- Headline: "Converta conversas do WhatsApp em reservas confirmadas no Bokun."
- Subheadline: "Instale uma vez, conecte seu WhatsApp e deixe o bot capturar horário, pickup e participantes — com confirmação e logs."
- Primary CTA button: "Começar teste grátis" — href="https://apps.bokun.io" (Bokun marketplace; placeholder URL to be updated when published)
- Secondary CTA button: "Ver como funciona" — onClick scrolls to the features section (anchor link #features)

Features section (LAND-02) — 4 benefit cards:
1. Title: "Reservas 24/7 no WhatsApp" | Description: "Bot atende clientes a qualquer hora, sem agente humano disponível."
2. Title: "Integração nativa com Bokun" | Description: "Instala via marketplace, sem configuração técnica."
3. Title: "Menos erros, mais conversão" | Description: "Fluxo guiado captura horário, pickup e participantes com precisão."
4. Title: "Controle e auditoria" | Description: "Dashboard com logs de conversa, lista de reservas e retry de webhooks falhos."

Icons for features (Claude's discretion per CONTEXT.md):
1. Clock (24/7)
2. Plug (integration)
3. TrendingUp (conversion)
4. LayoutDashboard (control)

Pricing section (LAND-03):
- Plan 1: "Mensal" | Price: "€29" | Period: "/mês" | Trial: "7 dias grátis incluídos" | CTA: "Começar teste grátis" | href: "/api/create-checkout-session?plan=monthly" (backend endpoint built in 04-04)
- Plan 2: "Anual" | Price: "€290" | Period: "/ano" | Highlight: "≈ 2 meses grátis" badge | Trial: "7 dias grátis incluídos" | CTA: "Começar teste grátis" | href: "/api/create-checkout-session?plan=annual" | Visual emphasis: highlighted border or "Recomendado" badge

App router context:
- Current App.tsx has RequireAuth wrapping all routes except /auth
- /landing or / must be PUBLIC (no auth required) — add it OUTSIDE the RequireAuth wrapper
- Auth flow: if unauthenticated user visits /, show LandingPage; if authenticated, still show LandingPage (do NOT auto-redirect authenticated users away — they may want to see pricing)
- Update the current catch-all: Route path="*" currently redirects to "/" which goes to /overview — change "/" to render LandingPage, and the existing redirect from "/" → "/overview" in RequireTenant should remain for authenticated+tenant users navigating within the app

From frontend/package.json — available libraries:
- tailwindcss v4, lucide-react, react-router-dom v7
- NO additional libraries needed for this page

From frontend/src/index.css — check if CSS custom properties define brand colors (use them for accent color if available).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create LandingPage.tsx with hero, features, and pricing sections</name>
  <files>
    frontend/src/pages/LandingPage.tsx
  </files>
  <action>
Create a complete, self-contained landing page component. No shared layout (no Sidebar) — this is a standalone page with its own nav bar or minimal header.

**Page structure:**
```
<div> (full-width, white background)
  <header> — minimal navbar: logo text "Bokun Bot" left, "Entrar" button right (href="/auth")
  <main>
    <section id="hero"> — hero section
    <section id="features"> — features grid
    <section id="pricing"> — pricing cards
  </main>
  <footer> — minimal: "© 2024 Bokun Bot. Todos os direitos reservados."
</div>
```

**Hero section:**
- Full-width section with light background (slate-50 or white with subtle gradient)
- Large centered headline (text-4xl md:text-5xl font-bold, max-w-3xl mx-auto)
- Subheadline below (text-lg text-slate-600, max-w-xl mx-auto)
- Two CTA buttons side-by-side on desktop, stacked on mobile (flex-col sm:flex-row gap-3 justify-center)
- Primary: filled button (bg-green-600 text-white hover:bg-green-700 px-6 py-3 rounded-lg)
- Secondary: outline button (border border-slate-300 text-slate-700 hover:bg-slate-100 px-6 py-3 rounded-lg)
- Secondary onClick: `() => document.getElementById("features")?.scrollIntoView({ behavior: "smooth" })`

**Features section (id="features"):**
- Section heading: "Por que usar o Bokun Bot?" centered
- 4-card grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6
- Each card: white background, rounded-xl, p-6, shadow-sm border border-slate-100
  - Icon in a colored circle (bg-green-100 p-3 rounded-full inline-flex mb-4)
  - Title: font-semibold text-slate-900
  - Description: text-sm text-slate-600

**Pricing section (id="pricing"):**
- Section heading: "Preços simples e transparentes" centered
- Two pricing cards side-by-side (grid-cols-1 md:grid-cols-2 gap-6, max-w-2xl mx-auto)
- Annual plan card gets a visual highlight: `ring-2 ring-green-500` and a "Recomendado" badge (absolute top -top-3 left-1/2 -translate-x-1/2)
- Each card shows: plan name (text-xl font-bold), price (text-4xl font-bold), period (/mês or /ano), trial note (text-sm text-slate-500), feature list (3 items: "Todas as funcionalidades", "Dashboard completo", "Suporte via email"), CTA button
- CTA buttons link to `/api/create-checkout-session?plan=monthly` and `/api/create-checkout-session?plan=annual`

**Mobile responsiveness (LAND-04):**
- All grid layouts use responsive breakpoints (grid-cols-1 → sm:grid-cols-2 → lg:grid-cols-4)
- Text sizes use responsive clamps (text-3xl md:text-5xl)
- CTA buttons stack on mobile (flex-col sm:flex-row)
- Pricing cards stack on mobile (grid-cols-1 md:grid-cols-2)
- Padding: px-4 sm:px-6 lg:px-8 on all sections
- No horizontal scroll: ensure max-w-7xl mx-auto wrapper on all sections
  </action>
  <verify>
    `cd /Users/luizbrazao/Projetos/Bokun/frontend && npm run build` — 0 TypeScript errors.
  </verify>
  <done>
    LandingPage.tsx exists with min 150 lines. Hero has exact headline/subheadline from CONTEXT.md. Features shows exactly 4 benefit cards. Pricing shows Monthly (€29) and Annual (€290) tiers with 7-day trial note. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire LandingPage as public route in App.tsx</name>
  <files>
    frontend/src/App.tsx
  </files>
  <action>
Update App.tsx routing to expose the landing page at the root `/` path as a PUBLIC route (no auth required):

Current App.tsx structure:
```tsx
<Routes>
  <Route path="/auth" element={<AuthRoute />} />
  <Route element={<RequireAuth />}>
    <Route path="/onboarding" element={<Onboarding />} />
    <Route element={<RequireTenant />}>
      <Route element={<DashboardLayout />}>
        <Route path="/" element={<Navigate to="/overview" replace />} />
        ...
      </Route>
    </Route>
  </Route>
  <Route path="*" element={<Navigate to="/" replace />} />
</Routes>
```

New structure:
1. Import `LandingPage` from `./pages/LandingPage`
2. Add `<Route path="/" element={<LandingPage />} />` OUTSIDE the RequireAuth wrapper (fully public)
3. Inside the DashboardLayout routes, change the existing `<Route path="/" element={<Navigate to="/overview" replace />} />` to `<Route index element={<Navigate to="/overview" replace />} />` — or simply remove it since authenticated users accessing "/" will still see the LandingPage (which is fine per CONTEXT.md note: "authenticated users can also visit it")
4. The catch-all `<Route path="*" element={<Navigate to="/" replace />} />` can remain as-is since "/" now renders LandingPage

Alternatively, keep it clean:
```tsx
<Routes>
  {/* Public routes */}
  <Route path="/" element={<LandingPage />} />
  <Route path="/auth" element={<AuthRoute />} />

  {/* Protected routes */}
  <Route element={<RequireAuth />}>
    <Route path="/onboarding" element={<Onboarding />} />
    <Route element={<RequireTenant />}>
      <Route element={<DashboardLayout />}>
        <Route path="/overview" element={<OverviewPage />} />
        ... (all other dashboard routes)
      </Route>
    </Route>
  </Route>

  <Route path="*" element={<Navigate to="/" replace />} />
</Routes>
```

Note: The AuthRoute component already handles redirecting authenticated+tenant users to "/overview", so authenticated users typing "/" will see the landing page rather than being auto-redirected. This is the intended behavior per CONTEXT.md.
  </action>
  <verify>
    `cd /Users/luizbrazao/Projetos/Bokun/frontend && npm run build` — 0 TypeScript errors. App.tsx imports LandingPage and registers the public "/" route.
  </verify>
  <done>
    App.tsx registers LandingPage at public route /. Unauthenticated users visiting / see the landing page. Dashboard routes remain protected. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/luizbrazao/Projetos/Bokun/frontend && npm run build` — 0 TypeScript errors
2. LandingPage.tsx exists with min 150 lines
3. App.tsx contains `<Route path="/" element={<LandingPage />}` outside RequireAuth
4. Hero uses exact headline: "Converta conversas do WhatsApp em reservas confirmadas no Bokun."
5. Features section has exactly 4 benefit cards
6. Pricing shows €29/mês and €290/ano tiers
7. Pricing CTAs link to /api/create-checkout-session?plan=monthly and plan=annual
</verification>

<success_criteria>
- LAND-01: Hero section with exact headline/subheadline from CONTEXT.md + "Começar teste grátis" CTA linking to Bokun marketplace
- LAND-02: Features section with exactly 4 benefit cards and icons
- LAND-03: Pricing section with Monthly €29 and Annual €290 tiers, each linking to Stripe Checkout
- LAND-04: Landing page is fully responsive on mobile — no horizontal scroll, responsive grids, stacked CTAs
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-landing-profile/04-03-SUMMARY.md`
</output>
